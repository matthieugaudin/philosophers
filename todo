
- monitor working with nb_meals

- change the formula to wait odd ? if inferior ?
- parsing
    -> error message on stderror
    -> overflow the int
    -> to much philos as an input to avoid segfault
    -> 0 as an input
    -> 1 philo input
- free data
- verify every malloc + pthread_create + pthread_mutex_init
- verify a function call or malloc fail
- put the code to the norme
- yt tests